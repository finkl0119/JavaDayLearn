* 14일차 수업

클래스 배열
class A {
}

A a[] = new A[10];
==> A라고 하는 클래스를 담을 수 있는 배열이 생성되고 그 안에는 모두 null이 들어있습니다.

a[0].a = 10;(X)
a[0] = new A();(O)


for(int i=0;<a.length;i++){

	a[i] = new A();
}


int b[] = new int[10];
b[0] = 10;(O)
